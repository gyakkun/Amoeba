<local:RestorableWindow x:Class="Amoeba.Interface.MainWindow"
        x:Name="MainWindows"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:local="clr-namespace:Amoeba.Interface"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="MainWindow"
        WindowSettings="{Binding Path=WindowSettings.Value, Mode=TwoWay}">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Closed">
            <i:InvokeCommandAction Command="{Binding CloseCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding Path=TabListBox_Width, Mode=TwoWay, Converter={StaticResource DoubleToGridLengthConverter}, FallbackValue=200}" />
            <ColumnDefinition Width="4" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" />
        <ListBox Name="TabListBox" Grid.Column="0" SelectionMode="Single" SelectedIndex="0" ItemsSource="{Binding Path=ViewModels}">
            <ListBox.Resources>
                <DataTemplate DataType="{x:Type local:InfoControlViewModel}">
                    <TextBlock Text="{Binding Path=MainWindow_Info, Source={StaticResource _languages}}" />
                </DataTemplate>
                <DataTemplate DataType="{x:Type local:StoreControlViewModel}">
                    <TextBlock Text="{Binding Path=MainWindow_Store, Source={StaticResource _languages}}" />
                </DataTemplate>
            </ListBox.Resources>
        </ListBox>
        <ContentControl Grid.Column="2" Content="{Binding SelectedItem, ElementName=TabListBox, Mode=OneWay}">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type local:InfoControlViewModel}">
                    <local:InfoControl DataContext="{Binding}" />
                </DataTemplate>
                <DataTemplate DataType="{x:Type local:StoreControlViewModel}">
                    <local:StoreControl DataContext="{Binding}" />
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
    </Grid>
</local:RestorableWindow>
