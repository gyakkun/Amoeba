<omnius:RestorableWindow x:Class="Amoeba.Interface.OptionsWindow"
        x:Name="OptionsWindows"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:local="clr-namespace:Amoeba.Interface"
        xmlns:omnius="clr-namespace:Omnius.Wpf;assembly=Omnius.Wpf"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="OptionsWindow"
        WindowSettings="{Binding Path=WindowSettings.Value, Mode=TwoWay}">
    <DockPanel>
        <TreeView DockPanel.Dock="Left" Name="TabTreeView">
            <TreeViewItem Header="{Binding Path=MainWindow_Info, Source={StaticResource Languages}}" DataContext="{Binding Path=TcpConnectionOptions}" />
        </TreeView>
        <ContentControl Content="{Binding SelectedItem, ElementName=TabTreeView, Mode=OneWay}">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type local:TcpConnectionOpitons}">
                    <local:StoreControl DataContext="{Binding}" />
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
    </DockPanel>
</omnius:RestorableWindow>
